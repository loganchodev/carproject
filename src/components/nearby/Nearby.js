import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  height: 100vh;
`;

const Nearby = () => {

  return (
    <Container>

    </Container>
  );
};

export default Nearby;
