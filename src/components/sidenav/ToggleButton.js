import styled from 'styled-components';

const ToggleButton = styled.button`
  padding-top: 20px;
  background: none;
  border: none;
  color: white;
  cursor: pointer;
  align-self: flex-end;
`;

export default ToggleButton;